# Дневной лог разработки - 2025-12-07



## UI (оформление)



### Коммит: 2ff42bf - "Update dependencies: Bootstrap 5.3.2 -> 5.3.8, Vue.js 3 -> 3.5.25; create daily log for 12-07"

- **Что сделано**: Обновлены зависимости проекта до последних стабильных версий: Bootstrap с 5.3.2 до 5.3.8, Vue.js с неопределенной версии `vue@3` до конкретной 3.5.25.

- **Как сделано**:
  * Проверены актуальные версии библиотек через веб-поиск в начале дня разработки (согласно правилам проекта).
  * Обновлены CDN ссылки в `index.html`: Bootstrap CSS и JS bundle до версии 5.3.8 с новыми integrity hash.
  * Обновлена ссылка на Vue.js с `vue@3` на `vue@3.5.25` для фиксации конкретной версии.
  * Обновлены отображаемые версии в UI: Bootstrap badge показывает 5.3.8, Vue.js версия в data() установлена на 3.5.25.
  * Создан файл дневного лога `history/2025/12-07.txt` для нового дня разработки.

- **Функциональность**: Приложение использует последние стабильные версии библиотек с исправлениями безопасности и улучшениями производительности. Фиксация конкретной версии Vue.js обеспечивает предсказуемость работы приложения.



### Коммит: 00cc0c6 - "Fix Bootstrap 5.3.8 integrity hash and switch Vue.js to production build"

- **Что сделано**: Исправлены неправильные integrity hash для Bootstrap 5.3.8, из-за которых библиотека не загружалась в браузере. Также переключен Vue.js на production-сборку для устранения предупреждения о development build.

- **Как сделано**:
  * Найдены правильные SHA-384 integrity hash для Bootstrap 5.3.8 через веб-поиск (официальные значения из документации Bootstrap).
  * Обновлен integrity hash для CSS: `sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB`.
  * Обновлен integrity hash для JS bundle: `sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI`.
  * Изменена ссылка на Vue.js с `vue.global.js` на `vue.global.prod.js` для использования production-сборки.

- **Функциональность**: Bootstrap теперь корректно загружается через CDN с проверкой целостности файлов. Vue.js работает в production-режиме без предупреждений в консоли, что улучшает производительность и соответствует best practices для production-окружения.



### Коммит: fedc28f - "Refactor: вынести Vue в отдельные обычные скрипты без сборки"

- **Что сделано**: Перенесена логика Vue-приложения из инлайна в отдельные обычные скрипты (`config.js`, `cmp-theme.js`, `cmp-perplexity-settings.js`, `cmp-chat.js`, `cmp-import-export.js`, `app.js`); `index.html` теперь подключает их классическими `<script>` без `type=\"module\"`. Добавлено правило в `.cursorrules` про учет особенностей GitHub Pages.

- **Как сделано**:
  * Сконфигурирован `window.appConfig` в `config.js` с дефолтной темой, моделью Perplexity и списком моделей.
  * Разнесены тематические блоки на компоненты: тема (`cmp-theme.js`), настройки Perplexity (`cmp-perplexity-settings.js`), чат (`cmp-chat.js`), импорт/экспорт настроек (`cmp-import-export.js`).
  * `app.js` собирает компоненты, монтирует Vue через `createApp`, применяет сохранённые значения из `localStorage`.
  * `index.html` теперь только подключает внешние скрипты, сохраняя работу через `file://` без сборки и без CORS-проблем.
  * В `.cursorrules` зафиксировано требование учитывать ограничения GitHub Pages (статический фронтенд, HTTPS, относительные пути).

- **Функциональность**: Приложение снова разнесено по отдельным файлам без сборки, совместимо с запуском напрямую из файловой системы (включая Android/OneDrive), настройки и чат Perplexity сохраняются в `localStorage`, поддерживается экспорт/импорт настроек.



### Коммит: d779f70 - "Add CoinGecko top-10 widget (price & % change)"

- **Что сделано**: Добавлен виджет CoinGecko топ-10 монет (BTC, ETH, SOL, BNB, XRP, ADA, DOGE, TRX, AVAX, DOT) с ценой и изменениями за 1h/24h/7d/30d/60d; кнопка ручного обновления, индикация загрузки/ошибок, отметка времени.

- **Как сделано**:
  * Новый компонент `cmp-coingecko.js` — запрос к `api.coingecko.com/coins/markets` с `price_change_percentage=1h,24h,7d,30d,60d`, хранение данных, форматирование цены/процентов, подсветка роста/падения.
  * Подключение компонента в `app.js` и вывод таблицы в `index.html` (Bootstrap-таблица, responsive).
  * Срабатывает при загрузке и по кнопке «Обновить», работает с `file://` без прокси и без ключей.

- **Функциональность**: В интерфейсе появился блок «Топ-10 (CoinGecko)» с актуальными ценами и процентными изменениями по основным монетам, обновляемый в один клик; данные грузятся напрямую с CoinGecko без сервера.



## UX (паттерны взаимодействий)



## MM (математическая модель)


