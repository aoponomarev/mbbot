# Дневной лог разработки - 2025-12-07


## UI (оформление)

### Коммит: 4f3def2 - "2025-12-07: Update dependencies, fix integrity hash, add daily log, update commit rules"

- **Что сделано**: Перешли на Bootstrap 5.3.8 и Vue.js 3.5.25 с корректными integrity-хешами и production-сборкой; создан дневной лог на 12-07; в `.cursorrules` зафиксировано правило про коммиты только по явному запросу.

- **Как сделано**:
  * Обновлены CDN-ссылки в `index.html` на Bootstrap 5.3.8 (CSS/JS) с валидными SHA-384 хешами.
  * Переключена загрузка Vue на `vue.global.prod.js` версии 3.5.25; отображаемая версия в UI обновлена.
  * Добавлен файл `history/2025/12-07.txt` для дневного лога.
  * В `.cursorrules` добавлено требование: коммиты только по явной команде пользователя.

- **Функциональность**: Библиотеки грузятся через CDN без предупреждений и с проверкой целостности; фиксированная версия Vue даёт предсказуемость; правило коммитов исключает авто-коммиты.


### Коммит: 8eba1ea - "Restore Vue init into external scripts (config.js/app.js)"

- **Что сделано**: Вынесена инициализация Vue из инлайна в обычные скрипты `config.js` и `app.js`, чтобы работать без сборки и с `file://`.

- **Как сделано**:
  * В `config.js` добавлен `window.appConfig` с дефолтными значениями темы, модели Perplexity и API-ключом по умолчанию.
  * В `app.js` создан `createApp` без `type="module"`: загрузка/сохранение темы и ключа в `localStorage`, форматирование сообщений, экспорт/импорт настроек, автопрокрутка чата.
  * `index.html` теперь подключает глобальный Vue и внешние скрипты `config.js`/`app.js` вместо большого инлайн-блока.

- **Функциональность**: Приложение снова запускается напрямую из файловой системы (OneDrive/Android) без CORS и сборки; настройки persist; интерфейс остаётся прежним.


### Коммит: 249aee0 - "Refactor: вынести Vue в отдельные обычные скрипты без сборки"

- **Что сделано**: Разбита логика приложения на отдельные модули-компоненты (`cmp-theme.js`, `cmp-perplexity-settings.js`, `cmp-chat.js`, `cmp-import-export.js`) и собрана в `app.js`; добавлено правило в `.cursorrules` про учёт GitHub Pages.

- **Как сделано**:
  * Тема, настройки Perplexity, чат и импорт/экспорт вынесены в независимые объекты-компоненты, подключаемые в `app.js`.
  * `app.js` агрегирует компоненты, подключает список моделей из конфига, монтирует Vue-приложение и синхронизирует с `localStorage`.
  * `index.html` подключает все скрипты как обычные `<script>` для совместимости с `file://`.
  * В `.cursorrules` зафиксировано требование учитывать ограничения GitHub Pages (статический фронтенд, относительные пути, HTTPS).

- **Функциональность**: Код модульный и читаемый; переключение темы/модели/ключа и чат работают через общий стейт; экспорт/импорт настроек сохранён; проект готов к статическому хостингу.


### Коммит: fa1e338 - "Add CoinGecko top-10 widget (price & % change)"

- **Что сделано**: Добавлен виджет CoinGecko с топ-10 монетами (BTC, ETH, SOL, BNB, XRP, ADA, DOGE, TRX, AVAX, DOT), ценами и изменениями за 1h/24h/7d/30d/60d; индикаторы загрузки/ошибки и кнопка обновления.

- **Как сделано**:
  * Создан компонент `cmp-coingecko.js`: запрос к `api.coingecko.com/coins/markets` с `price_change_percentage=1h,24h,7d,30d,60d`, форматирование цены/процентов и класс роста/падения.
  * Подключение компонента в `app.js`; добавлена таблица в `index.html` с responsive-обёрткой и отметкой времени.
  * Обновление данных при загрузке страницы и по кнопке «Обновить»; поддержка работы без ключей и прокси.

- **Функциональность**: В UI появился блок «Топ-10 (CoinGecko)» с актуальными ценами и процентной динамикой; пользователь обновляет данные в один клик, ошибки и загрузка отображаются явно.


## UX (паттерны взаимодействий)


## MM (математическая модель)
