# Настройки и правила проекта

## Коммит от 21.12.2025 : Configure Markdown syntax highlighting and file associations
◆ Настроена подсветка синтаксиса Markdown и ассоциации файлов. → Добавлено расширение `yzhang.markdown-all-in-one` в `.vscode/extensions.json`. Добавлены настройки Markdown All in One для улучшенной подсветки синтаксиса в `.vscode/settings.json`: `markdown.extension.syntax.plainTheme`, `markdown.extension.syntax.decorationFileSizeLimit`, `markdown.extension.toc.updateOnSave`, `markdown.extension.preview.autoShowPreviewToSide`. Добавлена ассоциация файлов `*.cursorrules` с языком `markdown` через `files.associations`. ◉ Улучшение работы с Markdown-файлами: расширение Markdown All in One обеспечивает полную подсветку синтаксиса, а ассоциация `.cursorrules` с Markdown позволяет использовать все возможности подсветки для файлов правил проекта.

## Коммит a6e5734 от 21.12.2025 : Start of day: 12-21
◆ Открыт новый рабочий день 21 декабря 2025. → Перемещены файлы документации в папку `docs/`: `architect.md`, `ui/guide-ii.md` → `docs/guide-ii.md`, `migration-plan.md`, `mm/median.md` → `docs/mm-median.md`. Обновлен `lastCommitMessage` в `core/cfg-app.js` на "Start of day: 12-21". ◉ Зафиксировано начало рабочего дня в истории репозитория.

## Коммит 8462463 от 20.12.2025 : Update commit hash rules and add mass edits protocol
◆ Обновлены правила работы с хешами коммитов в тематических логах и добавлен протокол массовых правок в одном файле. → В протоколе коммитов изменено правило: запись в тематические логи добавляется БЕЗ хеша при создании коммита, хеш добавляется в следующем коммите или при ревизии тематических логов. Добавлен протокол массовых правок в одном файле с описанием безопасного подхода через создание временных файлов `.new`. Обновлено определение "Тематические логи" в разделе ПОНЯТИЯ. Добавлено правило об обновлении оглавления при добавлении новых секций. Добавлены определения "Регламент" и "Протокол" в раздел ПОНЯТИЯ. ◉ Решение проблемы зацикливания через `git commit --amend`: хеши добавляются отсроченно, что предотвращает логический парадокс и обеспечивает трассируемость изменений. Протокол массовых правок заложил безопасный подход к множественным изменениям: создание временных файлов позволяет проверить результат перед заменой, что критично для предотвращения ошибок при массовых правках.

## Коммит 9c48ee4 от 20.12.2025 : + комиты
◆ Обновлен дневной лог за 18 декабря с добавлением записей о коммитах. → Обновлен `.cursorrules` с небольшими изменениями в правилах. Добавлены записи в `history/2025/12-18.txt` о коммитах дня. ◉ Дополнение дневного лога записями о коммитах для полноты истории.

## Коммит 773d320 от 20.12.2025 : Start of day: 12-20
◆ Открыт новый рабочий день 20 декабря 2025. → Обновлен `.cursorignore` и `.gitignore` с настройками игнорирования. Удален файл с заметками. Обновлен `lastCommitMessage` в `core/cfg-app.js` на "Start of day: 12-20". ◉ Зафиксировано начало рабочего дня в истории репозитория.

## Коммит 56b884b от 18.12.2025 : Start of day: 12-18
◆ Открыт рабочий день 18 декабря 2025 и зафиксировано исходное состояние проекта. → Проверены ключевые документы (`.cursorrules`, `architect.md`, `ui/guide-ii.md`, `mm/median.md`), состояние репозитория (ветка `master`, синхронизация с `origin/master`), актуальность зависимостей (Vue.js 3.5.25, Bootstrap 5.3.8, Font Awesome 6.5.1). Создан файл дневного лога `history/2025/12-18.txt`, обновлён `lastCommitMessage` в `core/cfg-app.js` на "Start of day: 12-18". ◉ Стандартизированный процесс открытия дня стал неотъемлемой частью workflow: фиксация исходного состояния проекта в начале каждого дня обеспечивает четкую точку отсчета для всех изменений дня, что критично для понимания контекста и восстановления состояния при необходимости.

## Коммит 5221bdd от 17.12.2025 : Start of day: 12-17
◆ Открыт новый рабочий день 17 декабря 2025. → Проведена проверка документации проекта (`.cursorrules`, `architect.md`, `ui/guide-ii.md`, `mm/median.md`). Проверено состояние репозитория (ветка `master`, синхронизация с `origin/master`). Проверены текущие версии зависимостей: Vue.js 3.5.25 (актуально), Bootstrap 5.3.8 (актуально), Font Awesome 6.5.1 (доступна 6.7.2; обновление не выполнялось). Добавлена привязка клавиш `Ctrl+K Ctrl+U` → `Simple Browser: Show` (workspace `.vscode/keybindings.json`). Актуализированы workspace-настройки поведения вторичной боковой панели/AI UI (`.vscode/settings.json`). Создан файл дневного лога `history/2025/12-17.txt`. Обновлён `lastCommitMessage` в `core/cfg-app.js` на "Start of day: 12-17". ◉ Расширение процесса открытия дня демонстрирует эволюцию: помимо стандартных проверок, процесс включает обновление настроек workspace и контроль доступных версий зависимостей, что показывает проактивный подход к поддержанию актуальности окружения. Добавление keyboard shortcuts показывает внимание к оптимизации workflow: часто используемые действия упрощаются через горячие клавиши.

## Коммит 599b851 от 16.12.2025 : Start of day: 12-16
◆ Открыт новый рабочий день 16 декабря 2025. → Проведена проверка документации проекта (`.cursorrules`, `architect.md`, `ui/guide-ii.md`, `mm/median.md`). Проверено состояние репозитория (чистое рабочее дерево, синхронизировано с `origin/master`). Проверены текущие версии зависимостей (Vue.js 3.5.25, Bootstrap 5.3.8, Font Awesome 6.5.1). Создан файл дневного лога `history/2025/12-16.txt`. Обновлен `lastCommitMessage` в `core/cfg-app.js` на "Start of day: 12-16". ◉ Стандартизированный процесс открытия дня стал рутиной: повторяющиеся проверки и фиксация состояния обеспечивают консистентность и предсказуемость начала каждой рабочей сессии, что критично для поддержания качества разработки и преемственности контекста между днями.

## Коммит 5b4c8ff от 16.12.2025 : Optimize .cursorrules: remove duplication, compress history, improve structure
◆ Оптимизирован файл `.cursorrules` для улучшения работы с контекстным окном ИИ-агента. → В `.cursorrules` удалено дублирование правил, сжата история изменений, улучшена структура документа. Обновлен `core/cfg-app.js` с новым значением `lastCommitMessage`. Добавлены записи в `history/2025/12-15.txt`. ◉ Предварительная оптимизация `.cursorrules` перед финальной версией, заложившая основу для дальнейшего улучшения структуры документа.

## Коммит 3e0a051 от 15.12.2025 : Optimize .cursorrules: remove duplication, remove history section, improve structure
◆ Оптимизирован файл `.cursorrules` для улучшения работы с контекстным окном ИИ-агента. → В разделе "Текущие предпочтения пользователя" заменены дублирующиеся правила на ссылки на основные разделы (например, "См. раздел 'Контроль качества выполнения' выше"). Полностью удален раздел "История изменений предпочтений" (строки 343-355). Убраны шаги "ДОБАВИТЬ запись в 'Историю изменений предпочтений'" из правил обработки просьб "на будущее" и команд UI:, UX:, MM:, II:, П:. Исправлена структура раздела о комментировании: список требований к комментариям перемещен в правильное место. Удалены избыточные повторения правил "Остановка при конфликте", "Чтение документации", "Контроль качества". ◉ Оптимизация `.cursorrules` демонстрирует практическое применение принципов оптимизации контекста: удаление дублирования и избыточной истории снижает размер файла, ускоряя загрузку и обработку. Замена дублирующихся правил на ссылки показывает принцип единственности источника истины: правило формулируется один раз, остальные места ссылаются на него. Удаление истории изменений демонстрирует понимание того, что историческая информация может отвлекать от актуальных правил, особенно для ИИ-агента, который должен фокусироваться на текущих требованиях, а не на процессе их формирования.

## Коммит 1b2f6cd от 15.12.2025 : Add rules for documentation formatting and context optimization
◆ Добавлен раздел "Правила оформления правил и документации" в `.cursorrules`, зафиксированы принципы оптимизации контекстного окна ИИ-агента как база для ведения документации. → Создан новый раздел "⚠️ Правила оформления правил и документации" после раздела "Сопровождение документации проекта". Добавлены 4 группы принципов: стратегия сжатия контекста, структурирование документации, использование загруженного контекста, применение к ведению документации. Сформулирована итоговая формула: "Загружать минимум необходимого, использовать максимум уже загруженного, структурировать для быстрого поиска". ◉ Фиксация принципов оптимизации контекстного окна демонстрирует мета-принцип: правила о том, как писать правила, заложили рекурсивное применение оптимизации. Это показывает понимание ограничений ИИ-агентов: контекстное окно ограничено, поэтому документация должна быть структурирована для эффективного использования. Формула "минимум необходимого, максимум загруженного" заложила практику экономии контекста: каждый байт контекста должен нести максимальную ценность, что критично для работы с большими проектами.

## Коммит f14d61d от 15.12.2025 : Start of day: 12-15
◆ Открыт новый рабочий день 15 декабря 2025. → Проведена проверка документации проекта (`.cursorrules`, `architect.md`, `ui/guide-ii.md`, `mm/median.md`). Проверено состояние репозитория (чистое рабочее дерево, синхронизировано с `origin/master`). Проверены текущие версии зависимостей (Vue.js 3.5.25, Bootstrap 5.3.8, Font Awesome 6.5.1). Создан файл дневного лога `history/2025/12-15.txt`. Обновлен `lastCommitMessage` в `core/cfg-app.js` на "Start of day: 12-15". ◉ Расширение списка проверяемых документов (добавлен `mm/median.md`) демонстрирует эволюцию процесса: по мере появления новых типов документации они включаются в стандартную процедуру проверки, что обеспечивает полноту контекста при начале работы. Это показывает принцип адаптивности: процесс открытия дня развивается вместе с проектом, включая новые области знаний.

## Коммит f16b9f7 от 14.12.2025 : Refactor documentation structure and add mandatory documentation reading principle
◆ Разгружен `.cursorrules` от деталей в пользу `architect.md` и `ui/guide-ii.md`, добавлен принцип обязательного чтения документации перед выполнением заданий. → Убраны детали работы с HTML файлами (стилизация, вынесение стилей, маркировка контейнеров, детерминированные хэши, стандартные размеры чекбоксов, система иконок) - оставлены только ссылки на `architect.md` и `ui/guide-ii.md`. Убраны детали проверки зависимостей - оставлена ссылка на `architect.md`. Убраны детали протоколирования - оставлена ссылка на `architect.md`. Убраны детали синхронизации настроек - оставлена ссылка на `architect.md`. Удалены разделы "Заметки для будущих сессий" и "Рекомендации для улучшения". Оставлены только принципиальные правила работы ИИ-агента. Обновлен раздел "Контроль качества выполнения" в `.cursorrules`: добавлен блок "⚠️ ПЕРЕД ВЫПОЛНЕНИЕМ ЛЮБОГО ЗАДАНИЯ ПОЛЬЗОВАТЕЛЯ" с требованиями прочитать соответствующую документацию проекта перед началом выполнения, применить рекомендации и предписания из документации, учесть существующие решения, паттерны и компоненты, уточнена цель: ИИ-агент должен читать и применять документацию проекта перед выполнением заданий. Добавлена запись в "Текущие предпочтения пользователя" с правилом "Чтение документации перед выполнением заданий". Добавлена запись в "Историю изменений предпочтений" с датой 2025-12-14. ◉ Разделение ответственности между документами демонстрирует принцип модульности: `.cursorrules` содержит только правила поведения агента, детали перенесены в специализированные документы, что упрощает навигацию и поддержку. Принцип обязательного чтения документации перед выполнением заданий заложил практику проактивного качества: агент должен понимать контекст и существующие решения перед началом работы, а не изобретать велосипед. Это критично для соответствия установленным принципам и предотвращения конфликтов с существующими паттернами.

## Коммит 6410c1e от 14.12.2025 : Start TODO
◆ Добавлены настройки VS Code для работы с задачами. → Обновлен `.vscode/extensions.json` с добавлением расширений. Обновлен `.vscode/settings.json` с настройками для работы с задачами. Создан `.vscode/tasks.json` с конфигурацией задач для автоматизации работы. ◉ Настройка системы задач в VS Code для автоматизации рутинных операций.

## Коммит 2722c35 от 14.12.2025 : Start of day: 12-14
◆ Открыт новый рабочий день 14 декабря 2025. → Проведена проверка документации проекта (`.cursorrules`, `architect.md`, `ui/guide-ii.md`). Проверено состояние репозитория (чистое рабочее дерево, синхронизировано с `origin/master`). Проверены текущие версии зависимостей (Vue.js 3.5.25, Bootstrap 5.3.8, Font Awesome 6.5.1). Создан файл дневного лога `history/2025/12-14.txt`. Обновлен `lastCommitMessage` в `core/cfg-app.js` на "Start of day: 12-14". ◉ Стандартизированный процесс открытия рабочего дня обеспечивает консистентность: каждый день начинается с одинакового набора проверок и фиксации состояния, что критично для поддержания качества и преемственности разработки. Это демонстрирует принцип ритуализации: повторяющиеся процессы формализуются и становятся частью стандартного workflow, снижая вероятность пропуска важных шагов.

## Коммит 56ca9ca от 13.12.2025 : Add rule for commit hash requests in daily log
◆ Добавлено правило о просьбах добавить хеш коммита в дневной лог. → Добавлено правило в раздел "ОБЯЗАТЕЛЬНЫЙ ЧЕКЛИСТ ПЕРЕД КАЖДЫМ КОММИТОМ" в `.cursorrules`. Правило запрещает изменение дневного лога при просьбах добавить хеш коммита (фразы типа "добросить", "добавить хеш"). ИИ-агент должен объяснять, что хеши добавляются только при завершении рабочего дня во время ревизии. Исправлено дублирование строки "**Что сделано**" в дневном логе. ◉ Запрет на добавление хешей по запросу демонстрирует понимание временных зависимостей: хеши добавляются только при ревизии дневного лога, что является отдельным процессом от создания коммитов. Это предотвращает нарушение процесса документирования и исключает циклические зависимости, когда попытка добавить хеш в лог создает незакоммиченные изменения, которые требуют нового коммита. Правило заложило практику разделения процессов: коммит фиксирует изменения, ревизия уточняет детали.

## Коммит 405093d от 13.12.2025 : Start of day: 12-13
◆ Открыт новый рабочий день 13 декабря 2025. → Проведена проверка документации проекта (`.cursorrules`, `architect.md`, `ui/guide-ii.md`). Проверено состояние репозитория (чистое рабочее дерево, синхронизировано с `origin/master`). Проверены текущие версии зависимостей (Vue.js 3.5.25, Bootstrap 5.3.8, Font Awesome 6.5.1). Добавлено правило об обязательном создании открывающего коммита при открытии нового рабочего дня в `.cursorrules`. Создан файл дневного лога `history/2025/12-13.txt`. Обновлен `lastCommitMessage` в `core/cfg-app.js` на "Start of day: 12-13". ◉ Формализация процесса открытия рабочего дня демонстрирует принцип структурированности: каждый день начинается с проверки состояния проекта и фиксации этого момента в истории, что обеспечивает четкую границу между рабочими сессиями и упрощает навигацию по истории разработки. Это заложило практику ритуализации начала работы: проверка документации, состояния репозитория и зависимостей становится стандартным процессом, гарантирующим преемственность и актуальность контекста.

## Коммит db226c7 от 12.12.2025 : Add rule for understanding 'внеси в правила' command
◆ Добавлено правило понимания команды "внеси в правила" с распределением изменений по соответствующим документам. → Добавлен раздел "КРИТИЧЕСКИ ВАЖНО - Понимание команды 'внеси в правила'" в секцию "Сопровождение документации проекта" в `.cursorrules`. Правило определяет, что при команде "внеси в правила" не следует автоматически предполагать, что речь идет только о `.cursorrules`. Правило требует анализа содержания просьбы и определения типа правил (правила работы ИИ-агента → `.cursorrules`, архитектурные правила → `architect.md`, правила UI/UX → `ui/guide-ii.md`, смешанные → декомпозировать и разместить в соответствующих документах). Добавлено краткое напоминание в раздел "Стиль работы" со ссылкой на детальное правило. Добавлено в раздел "Текущие предпочтения пользователя" для фиксации предпочтения. Добавлена запись в "Историю изменений предпочтений" с датой 2025-12-12. ◉ Правило понимания команды "внеси в правила" демонстрирует принцип контекстуальной интерпретации: команда не имеет фиксированного значения, а требует анализа содержания для определения правильного места размещения правила. Это заложило практику умной классификации: правила распределяются по документам в зависимости от их типа, что критично для поддержания структурированности документации. Декомпозиция смешанных правил показывает понимание модульности: одно правило может затрагивать несколько областей, и каждая часть должна быть размещена в соответствующем документе.

## Коммит 07c6c3a от 11.12.2025 : Fix date handling: use Moscow timezone and correct log date
◆ Исправлена дата дневного лога с 12-12 на 12-11, добавлено правило определения даты по московскому времени для дневных логов и документации, исправлены даты в истории изменений предпочтений. → Файл дневного лога переименован с `history/2025/12-12.txt` на `history/2025/12-11.txt`, дата в заголовке исправлена на 2025-12-11. В `.cursorrules` добавлено правило определения даты по московскому времени (MSK, UTC+3) для дневных логов и всех дат в документации проекта. ИИ-агент должен учитывать московский часовой пояс при создании файла дневного лога, указании даты в заголовке и при заносе дат в любую документацию (`.cursorrules`, `architect.md`, `ui/guide-ii.md` и другие документы). В разделе "История изменений предпочтений" исправлены даты с `2025-12-12` на `2025-12-11` для трех записей: правило ознакомления с документацией, правило терминологии "Друг", команда П:. ◉ Фиксация московского часового пояса как стандарта демонстрирует принцип единообразия: все даты в документации определяются по одному часовому поясу, что исключает путаницу и обеспечивает консистентность. Это критично для корректной организации дневных логов и понимания хронологии изменений. Правило заложило практику явного указания часового пояса: не полагаться на системные настройки, а явно указывать MSK во всех документах.

## Коммит 560920e от 11.12.2025 : Update documentation: add new rules, remove BOT references, remove metrics caching
◆ Добавлены новые правила работы ИИ-агента (ознакомление с документацией при открытии дня, терминология "Друг", команда П:). → В `.cursorrules` добавлен раздел "При открытии нового рабочего дня" с правилом обязательного ознакомления с документацией (`.cursorrules`, `architect.md`, `ui/guide-ii.md`) при открытии нового рабочего дня, проверки актуальности зависимостей и состояния проекта. В `.cursorrules` добавлено правило терминологии "Друг": когда пользователь упоминает "Друга" (с большой буквы), имеется в виду размышляющая модель Sonet 4.5, с которой у пользователя сложились хорошие отношения и доверие. В `.cursorrules` добавлена команда П: для фиксации принципов и глобальных концептов. Команда требует тщательного анализа принципа, декомпозиции идеи и размещения документации в одном или нескольких документах, где это наиболее уместно (`.cursorrules`, `architect.md`, `ui/guide-ii.md` и другие). ◉ Правило обязательного ознакомления с документацией при открытии дня демонстрирует принцип контекстуализации: каждый рабочий день начинается с полного понимания текущего состояния проекта, правил и архитектуры, что критично для качественного выполнения задач. Терминология "Друг" показывает внимание к контексту общения: понимание специфических терминов пользователя улучшает коммуникацию и предотвращает недопонимание. Команда П: заложила практику систематизации знаний: принципы и концепты фиксируются в момент их формулирования, что предотвращает потерю важных решений и обеспечивает их применение в будущем.

## Коммит 686f166 от 11.12.2025 : Add old_app_not_write to gitignore and document as read-only examples
◆ Добавлена папка `old_app_not_write/` в `.gitignore` и задокументировано правило работы с файлами из этой папки как с примерами кода. → Добавлено правило `old_app_not_write/` в `.gitignore`. Добавлено правило в `.cursorrules` о работе с файлами из папки `old_app_not_write/`: файлы рассматриваются только как примеры кода для понимания логики или структуры, никогда не редактируются, не изменяются, не коммитятся. При использовании кода из этих файлов - только читать для понимания, затем реализовывать аналогичную логику в файлах текущего проекта. Обновлен `core/cfg-app.js` с новым значением `lastCommitMessage`. ◉ Исключение папки `old_app_not_write/` из версионного контроля и фиксация правила работы с ней демонстрирует принцип изоляции: старые файлы используются только как источники знаний, а не объекты для редактирования, что критично для защиты примеров кода от случайных изменений.

## Коммит 0a67ba3 от 10.12.2025 : Create guide-ii.md and integrate with project documentation
◆ Добавлены правила контроля качества и сопровождения документации. → В `.cursorrules` добавлен раздел "Сопровождение документации проекта" с правилами обновления документации при изменениях в проекте (список документов: `.cursorrules`, `architect.md`, `ui/guide-ii.md`, исключение: папка `docs/`). В `.cursorrules` добавлен раздел "Контроль качества выполнения" в начало файла с требованием проверки соответствия документации после каждой задачи и самостоятельного исправления ошибок. Усилено правило о тщательной проверке согласованности: добавлено требование проверять документацию насквозь, а не "кусками", обновлять структуру проекта при изменениях, не допускать дублирования. ◉ Создание системы контроля качества демонстрирует принцип проактивности: ИИ-агент не просто выполняет задачи, но и самостоятельно проверяет их качество и соответствие документации. Это заложило практику самоконтроля: агент должен исправлять ошибки без запроса пользователя, что критично для поддержания высокого качества разработки. Требование проверять документацию "насквозь" показывает понимание важности целостности: документация должна быть согласована во всех частях, а не только в измененных разделах.

## Коммит 2605d20 от 09.12.2025 : Complete daily log entries and enforce full documentation before commit
◆ Добавлены недостающие записи в дневной лог для коммита "Improve footer mobile height and align market metrics with source", усилено правило о полном описании всех изменений в дневном логе перед коммитом. → В `history/2025/12-09.txt` добавлена запись для MM (математическая модель) с описанием выравнивания переменных метрик рынка с первоисточником. В `history/2025/12-09.txt` добавлена запись для Architect с описанием правила работы с переменными первоисточников. В `.cursorrules` в раздел "ШАГ 2: Записать в дневной лог" добавлено критически важное требование: в дневной лог должны быть внесены **ВСЕ** изменения из коммита. Если в коммите несколько изменений (например, UI, MM, Architect), то каждое изменение должно быть описано в соответствующей секции дневного лога. **НЕ создавать коммит, пока все изменения не описаны в дневном логе!** ◉ Усиление требования полного описания изменений демонстрирует принцип трассируемости: каждый коммит должен быть полностью задокументирован в дневном логе, что критично для восстановления контекста и понимания истории разработки. Это заложило практику "документация как часть коммита": коммит считается неполным без соответствующей записи в логе, что предотвращает потерю информации о причинах и контексте изменений. Правило особенно важно для многокомпонентных коммитов, где изменения затрагивают несколько областей проекта.

## Коммит 98f598d от 09.12.2025 : Revise documentation terminology and add Bootstrap prioritization rule
◆ Проведена ревизия жаргона и сомнительных формулировок в `.cursorrules` и `architect.md`, заменены на профессиональную техническую терминологию. Добавлено правило о минимизации кастомности и приоритете использования Bootstrap для стилизации. Добавлено правило о профессиональной терминологии в документации. → В `.cursorrules` заменены жаргонные выражения: "болтаются просто так" → "не используются в проекте", "догоняния хешей" → "синхронизации хешей коммитов с дневным логом", "замкнутый круг" → "циклическая зависимость", "больше конкретики, меньше воды" → "конкретные технические детали, избегать избыточных описаний", "попадать в знакомую среду" → "обеспечить идентичную среду разработки", "подробно, но разумно" → "с достаточной детализацией", "сущность и предназначение" → "назначение и семантика". В `architect.md` заменены жаргонные выражения: "данные тянем из публичных API" → "получение данных из публичных API", "таймсерии" → "временные ряды", "chunked загрузка" → "поэтапная загрузка", "delta/RLE" → "дельта-сжатие или RLE-кодирование", "родственного назначения" → "однородного назначения", "специализация понятна" → "специализация определяется". Добавлено правило в раздел "При работе с файлами" о приоритете использования Bootstrap классов и утилит, минимизации кастомных CSS стилей, inline стилей и секций `<style>`. Добавлено правило в раздел "Пользовательские предпочтения" о профессиональной терминологии в документации: документация предназначена для ИИ-агентов, использовать профессиональную техническую терминологию, избегать жаргона, сленга и разговорных выражений, формулировки должны быть точными и технически корректными. Обновлена история изменений предпочтений с записью о ревизии жаргона и добавлении правил. ◉ Ревизия терминологии демонстрирует принцип профессиональной коммуникации: документация для ИИ-агентов должна использовать точную техническую лексику, исключая двусмысленности и неоднозначности, которые могут привести к неправильной интерпретации правил. Это заложило стандарт качества документации: каждый термин должен быть технически корректен и однозначен. Правило приоритета Bootstrap показывает баланс между гибкостью и стандартизацией: использование готовых компонентов обеспечивает единообразие и упрощает поддержку, минимизируя кастомный код, который может стать источником багов.

## Коммит 2605d20 от 09.12.2025 : Add rule for working with source variables and code comments
◆ Добавлено правило в `.cursorrules` о работе с переменными первоисточников и комментировании кода при переносе из других проектов. → В `.cursorrules` в раздел "Текущие предпочтения пользователя" добавлено правило: использовать переменные первоисточников в приоритете, при конфликтах имен сообщать пользователю подробно, но разумно, подробно (но разумно) переносить комментарии первоисточников, всегда подробно комментировать переменные при их объявлении (о сущности и предназначении). Добавлена запись в "Историю изменений предпочтений" с датой 2025-12-09 и описанием правила. ◉ Правило сохранения имен переменных и комментариев из первоисточников демонстрирует принцип совместимости: при переносе математических моделей важно сохранять оригинальную терминологию для корректной работы алгоритмов и понимания логики. Это заложило практику уважения к исходному коду: комментарии и имена переменных несут важную семантическую информацию, которую нельзя терять при переносе. Правило критично для работы с математическими моделями, где точность терминологии определяет корректность вычислений.

## Коммит 2f53a00 от 09.12.2025 : Add Architect section to daily logs and create docs folder
◆ Добавлена секция Architect в структуру дневных логов, проведена ревизия всех существующих логов с перемещением записей, связанных с архитектурой и настройками, создана папка `docs/` для справочных материалов, добавлено упоминание о будущей секции DOCS. → В `.cursorrules` обновлено описание структуры дневного лога: добавлена секция Architect (архитектура, настройки), упомянута будущая секция DOCS (документация). Проведена ревизия всех дневных логов (12-06, 12-07, 12-08, 12-09): добавлена секция Architect, перемещены записи, связанные с настройками `.cursorrules`, `.vscode/settings.json`, регламентами коммитов, структурой проекта, архитектурными изменениями. В пустые секции MM добавлены пометки об отсутствии изменений. Создана пустая папка `docs/` в корне проекта для будущих справочных материалов. Обновлена история изменений предпочтений в `.cursorrules`. ◉ Расширение структуры дневных логов демонстрирует эволюцию системы категоризации: добавление секции Architect отражает понимание, что настройки и архитектурные решения требуют отдельной категории для лучшей навигации. Ревизия существующих логов показывает принцип ретроспективной структуризации: старые записи реорганизуются под новую структуру, что критично для консистентности и удобства поиска. Создание папки `docs/` заложило основу для будущего расширения системы документации, демонстрируя проактивный подход к организации знаний.

## Коммит f6d5b1d от 09.12.2025 : Move architectural rules to architect.md and improve Markdown preview
◆ Перенесены архитектурные правила из `.cursorrules` в `architect.md`, добавлена настройка для автоматического открытия Markdown файлов в режиме preview, улучшено форматирование путей в архитектурном плане. → В `architect.md` добавлены разделы: "Стандарты разработки" (кодировка, окончания строк, HTML, стилизация), "Технические ограничения" (порядок загрузки x-template, размещение компонентов, внешние запросы), "Принципы разработки" (UI и MM разделы для добавления принципов по командам), "Обратная совместимость данных" (правило с примерами), обновлена "Структура проекта" с учетом новой организации папок. В `.cursorrules` заменены архитектурные разделы на ссылки на соответствующие разделы в `architect.md`, добавлено правило отслеживания согласованности между двумя файлами, обновлена обработка команд UI:/UX:/MM: для обновления разделов в `architect.md`. В `.vscode/settings.json` добавлена настройка `workbench.editorAssociations` для автоматического открытия Markdown файлов в режиме preview (`*.md` → `vscode.markdown.preview.editor`). В `architect.md` обновлено форматирование путей в разделе "Текущее размещение файлов": пути выделены жирным шрифтом для лучшей читаемости. ◉ Разделение ответственности между `.cursorrules` и `architect.md` демонстрирует принцип модульности документации: правила поведения агента отделены от архитектурных принципов, что упрощает навигацию и поддержку. Централизация архитектурных правил в одном документе заложила практику единого источника истины для архитектуры: все принципы, стандарты и ограничения находятся в одном месте, что критично для консистентности. Автоматическое открытие Markdown в режиме preview показывает внимание к удобству работы: форматированный текст читается лучше, чем сырой Markdown, что улучшает восприятие документации.

## Коммит 3b8d134 от 09.12.2025 : Add rule for handling files outside project scope
◆ Добавлено правило в `.cursorrules` о работе с файлами за пределами проекта, чтобы предотвратить случайное редактирование файлов из других проектов. → В `.cursorrules` добавлен раздел "⚠️ КРИТИЧЕСКИ ВАЖНО - Работа с файлами за пределами проекта" в секцию "Общая информация о проекте". Определено, что файлы за пределами проекта (вне папки `D:\Clouds\AO\OneDrive\Portfolio - CV\Refactoring\ToDo\Statistics\BOT`) рассматриваются только как примеры кода для понимания логики или структуры. Добавлены запреты: никогда не редактировать, не изменять, не коммитить файлы за пределами проекта, никогда не добавлять файлы из других проектов в staging или коммиты текущего проекта. Уточнено, что при использовании кода из файлов других проектов - только читать их для понимания, затем реализовывать аналогичную логику в файлах текущего проекта. Указано, что если пользователь предоставляет файл из другого проекта - это пример для изучения, а не для редактирования. ◉ Установлена четкая граница ответственности: файлы за пределами проекта рассматриваются только как источники знаний, а не объекты для редактирования. Это демонстрирует принцип изоляции проектов: каждый проект должен быть защищен от непреднамеренных изменений извне, что критично для целостности других проектов пользователя. Правило заложило практику безопасного изучения кода: можно читать и понимать, но реализовывать нужно в рамках текущего проекта, что предотвращает случайное нарушение границ между проектами.

## Коммит 5059249 от 08.12.2025 : Fix x-template loading order and sync VS Code settings
◆ Исправлен порядок загрузки x-template шаблона (сплэш не отображался), добавлено правило в `.cursorrules`, синхронизированы настройки VS Code, настроен упрощенный процесс staging через keyboard shortcut. → В `index.html` перемещен `<script type="text/x-template" id="splash-template">` из конца файла в начало - перед загрузкой Vue.js и компонентов. В `.cursorrules` добавлено правило "⚠️ Порядок загрузки x-template шаблонов" в раздел "Технические особенности" с описанием правильного порядка: Bootstrap JS → x-template шаблоны → Vue.js → компоненты → app-ui-root.js. Добавлено правило "Автоматический staging в Cursor/VS Code". Создан `.vscode/settings.json` с настройками Git, автосохранения файлов, редактора. Создан `.vscode/keybindings.json` с горячей клавишей `Ctrl+Shift+S` для быстрого staging всех изменений. Создан `.vscode/extensions.json` с рекомендациями расширений для работы с Git. ◉ Фиксация порядка загрузки x-template шаблонов демонстрирует понимание зависимостей инициализации: шаблоны должны быть доступны в DOM до монтирования Vue-компонентов, иначе они не могут быть использованы. Это заложило принцип явных зависимостей: порядок загрузки формализован в правилах, предотвращая подобные ошибки в будущем. Автоматизация staging через keyboard shortcut показывает подход к оптимизации рутинных операций: часто используемые действия упрощаются, экономя время и снижая вероятность ошибок.

## Коммит 0dd9d6d от 08.12.2025 : Unify UX into UI: remove separate UX prefix and section
◆ Объединение UX с UI в единую систему: удален префикс `ux-` из системы именования файлов, секция UX объединена с UI в `.cursorrules`, добавлены пояснения что UX является частью UI. → В `architect.md` удален префикс `ux-` из списка секторов, обновлено описание: `ui-` теперь включает "оформление и взаимодействие/UX". В `.cursorrules` объединены секции "UI (оформление)" и "UX (паттерны взаимодействий)" в единую секцию "UI (интерфейс)" с описанием "оформление, взаимодействие (UX), анимации, accessibility", обновлено правило обработки команд: команды `UI:` и `UX:` обе направляются в раздел "UI (интерфейс)". ◉ Унификация UX и UI демонстрирует понимание природы веб-компонентов: оформление и взаимодействие неразделимы в реальной реализации, поэтому искусственное разделение на префиксы создавало лишнюю сложность. Это заложило принцип практичности над формальностью: система именования должна отражать реальность разработки, а не абстрактные концепции. Упрощение правил улучшает навигацию и снижает когнитивную нагрузку при принятии решений о классификации файлов.

## Коммит 6b5544a от 08.12.2025 : Add security layer with obfuscation and API key field in splash
◆ Добавлена архитектура безопасности с обфускацией чувствительных данных (PIN, API-ключи), поле ввода API-ключа Perplexity в сплэш-экране, система префиксов `sec-` для security-модулей, правило обратной совместимости данных. → Создана папка `core/security/` и модуль `u-sec-obfuscate.js` с методами обфускации/деобфускации через Base64. В `architect.md` добавлен слой `core/security/`, сектор `sec-` в систему префиксов. В `.cursorrules` добавлено правило "⚠️ ПРАВИЛО: Обратная совместимость данных" - при изменении структуры сохраненных данных ИИ-агент должен предлагать реализацию обратной совместимости с объяснением рисков и примерами кода. Переименовано `apiKey` → `apiKeyPerplexity` во всех компонентах для явности и готовности к расширению. ◉ Создан архитектурный слой безопасности, демонстрирующий принцип защиты чувствительных данных: обфускация через Base64 предотвращает простой просмотр PIN и API-ключей в localStorage и экспортах, что критично для защиты пользовательских данных. Правило обратной совместимости заложило практику миграции данных: при изменении структуры сохраненных данных система должна поддерживать старые форматы, предотвращая потерю пользовательских настроек. Это показывает баланс между безопасностью и удобством: данные защищены, но остаются доступными для пользователя.

## Коммит 0b6acf5 от 08.12.2025 : Add mandatory checklist for commit workflow
◆ Усилен регламент коммитов добавлением яркого обязательного чеклиста с 4 шагами, где Шаг 3 (обновление `lastCommitMessage`) выделен как критически важный. → В `.cursorrules` заменён раздел "Шаги (обязательные)" на новый блок "⚠️ ОБЯЗАТЕЛЬНЫЙ ЧЕКЛИСТ ПЕРЕД КАЖДЫМ КОММИТОМ". Шаг 3 выделен жирным текстом, символами предупреждения и множественными напоминаниями о необходимости обновлять `lastCommitMessage` в `core/cfg-app.js`. Добавлены конкретные инструкции: открыть файл, найти строку, заменить значение, добавить в staging. Финальное напоминание: "ЭТОТ ШАГ НЕЛЬЗЯ ПРОПУСКАТЬ!" и "без обновления lastCommitMessage коммит считается неполным". Обновлена переменная `lastCommitMessage` в `core/cfg-app.js` с устаревшего значения на актуальное. Добавлена запись в "Историю изменений предпочтений" о причине усиления правил. ◉ Визуально выделенный чеклист демонстрирует принцип предотвращения ошибок через явные напоминания: критически важные шаги выделяются визуально и текстом, что снижает вероятность их пропуска. Множественные напоминания о Шаге 3 показывают понимание человеческой природы: даже важные действия могут быть забыты, поэтому их нужно подчеркивать многократно. Это заложило практику защитного программирования для процессов: не полагаться на память, а использовать явные инструкции и визуальные маркеры.

## Коммит 60b5d7e от 08.12.2025 : Update commit rules: no immediate hash in log, no amend
◆ Обновлён регламент создания коммитов: запрещено сразу добавлять хеш в лог и использовать `git commit --amend` для этого, так как это создаёт замкнутый круг. Хеши обновляются при ревизии дневного лога. → В шаге 2 регламента добавлено требование указывать в логе только название коммита без хеша. В шаге 5 регламента добавлен запрет на использование `git commit --amend` для добавления хеша и указано, что хеши обновляются при ревизии лога. Обновлён раздел протоколирования: убрано требование всегда указывать хеш, указано, что хеш добавляется при ревизии. Обновлена история изменений предпочтений с уточнением нового регламента. ◉ Разделение процесса коммита и добавления хешей демонстрирует понимание временных зависимостей: хеш коммита появляется только после его создания, поэтому попытка добавить его в сам коммит создает логический парадокс. Ревизия дневного лога как отдельный этап заложила практику двухфазного документирования: сначала фиксируются изменения, затем уточняются детали, что упрощает процесс и предотвращает ошибки.

## Коммит ae87fbb от 08.12.2025 : Add lastCommitMessage display and update commit rules
◆ Добавлено отображение названия последнего коммита в интерфейсе рядом с заголовком, обновлён регламент создания коммитов с выделением шага обновления `lastCommitMessage` в отдельный пункт. → Добавлена переменная `lastCommitMessage` в `core/cfg-app.js` с текущим значением. В `app/app-ui-root.js` добавлено чтение `lastCommitMessage` из конфига и передача в data() приложения. В `index.html` добавлено отображение `lastCommitMessage` под заголовком с иконкой ветки Font Awesome. В `.cursorrules` обновлён регламент коммитов: шаг 3 — обновление `lastCommitMessage` в `core/cfg-app.js`, остальные шаги перенумерованы (4, 5). ◉ Отображение последнего коммита в интерфейсе демонстрирует принцип прозрачности: пользователь всегда видит актуальное состояние проекта и может быстро понять, какие изменения были внесены. Интеграция обновления `lastCommitMessage` в регламент коммитов заложила практику синхронизации: код и документация обновляются вместе, предотвращая рассинхронизацию между фактическим состоянием и отображаемой информацией.

## Коммит ba3f222 от 08.12.2025 : Adopt file prefix naming and rename scripts
◆ Добавлены правила именования файлов с префиксами, зафиксировано требование поддерживать пул префиксов в `.cursorrules`/`architect.md`, переименованы существующие скрипты под новую схему и обновлён `index.html`. → Пополнены правила коммитов: фиксация имени до коммита, запись описания в дневной лог, добавление хеша после коммита. В `architect.md` описан желаемый пул префиксов и расширено требование его поддерживать. В `.cursorrules` добавлены шаги регламента коммитов и напоминание про префиксы. Переименованы `config.js`, `cmp-*.js`, `app.js` в `cfg-app.js`, `c-ui-*.js`, `app-ui-root.js`; пути обновлены в `index.html`. ◉ Система префиксов именования файлов заложила принцип самодокументируемости кода: по имени файла сразу понятна его роль и место в архитектуре (cfg- для конфигурации, c-ui- для UI-компонентов, app- для приложения). Это демонстрирует подход к масштабируемости: единое соглашение упрощает навигацию в растущей кодовой базе и помогает ИИ-агенту правильно классифицировать файлы. Формализация регламента коммитов обеспечивает воспроизводимость процесса: каждый коммит проходит через стандартизированные шаги, что гарантирует полноту документации и корректность истории.

## Коммит 60c91b7 от 08.12.2025 : Document splash hash
◆ Обновлена документация в дневном логе о хешировании пароля сплэша. → В `history/2025/12-08.txt` обновлена запись о сплэш-экране с уточнением о хешировании. ◉ Уточнение документации о механизме безопасности демонстрирует принцип актуализации знаний: документация должна отражать реальную реализацию, особенно в критичных областях вроде безопасности. Это обеспечивает правильное понимание системы защиты и предотвращает неправильные предположения о механизме хеширования.

## Коммит a3040ed от 08.12.2025 : Clarify commit amend policy
◆ Уточнена политика использования `git commit --amend` в правилах коммитов. → В `.cursorrules` добавлено уточнение о запрете использования `git commit --amend` для добавления хешей в лог. ◉ Запрет на использование `git commit --amend` для добавления хешей демонстрирует понимание циклических зависимостей: попытка добавить хеш коммита в сам коммит создает логический парадокс. Это заложило принцип разделения ответственности: коммит фиксирует изменения, а хеши добавляются позже при ревизии, что упрощает процесс и предотвращает ошибки.

## Коммит 696c3e0 от 08.12.2025 : gitignore
◆ Обновлен `.gitignore` для исключения файлов VS Code, добавлены настройки для автоматического staging. → В `.gitignore` добавлены исключения для `.vscode/*`, но разрешены конкретные файлы конфигурации (settings.json, extensions.json и т.д.). Добавлены файлы `.vscode/keybindings.json` и `.vscode/tasks.json` для автоматизации работы с Git. ◉ Селективное игнорирование файлов VS Code демонстрирует баланс между защитой репозитория от личных настроек и необходимостью синхронизации конфигурации: системные файлы игнорируются, но важные настройки версионируются. Автоматизация staging через tasks показывает подход к оптимизации рутинных операций: часто используемые действия формализуются и упрощаются, экономя время разработчика.

## Коммит 74e1e4c от 08.12.2025 : Fix commit hash in history
◆ Исправлен хеш коммита в дневном логе с неправильного `615180b` на корректный `495d826`. → В файле `history/2025/12-08.txt` обновлена запись коммита "Show last commit name on splash" с правильным хешом. ◉ Исправление хеша коммита демонстрирует внимание к точности документации: корректные ссылки на коммиты критичны для трассируемости изменений и восстановления контекста. Это показывает важность регулярной ревизии документации для поддержания её достоверности и полезности.

## Коммит 249aee0 от 07.12.2025 : Refactor: вынести Vue в отдельные обычные скрипты без сборки
◆ Разбита логика приложения на отдельные модули-компоненты и собрана в `app.js`; добавлено правило в `.cursorrules` про учёт GitHub Pages. → Тема, настройки Perplexity, чат и импорт/экспорт вынесены в независимые объекты-компоненты, подключаемые в `app.js`. `app.js` агрегирует компоненты, подключает список моделей из конфига, монтирует Vue-приложение и синхронизирует с `localStorage`. `index.html` подключает все скрипты как обычные `<script>` для совместимости с `file://`. В `.cursorrules` зафиксировано требование учитывать ограничения GitHub Pages (статический фронтенд, относительные пути, HTTPS). ◉ Модульная архитектура без сборки демонстрирует принцип простоты: отказ от сложных инструментов сборки в пользу нативных возможностей браузера упрощает разработку и отладку, сохраняя при этом читаемость и структурированность кода. Учет ограничений GitHub Pages заложил практику разработки с учетом целевой платформы: код изначально пишется совместимым со статическим хостингом, что исключает проблемы при развертывании.

## Коммит 4f3def2 от 07.12.2025 : Update dependencies, fix integrity hash, add daily log, update commit rules
◆ Перешли на Bootstrap 5.3.8 и Vue.js 3.5.25 с корректными integrity-хешами и production-сборкой; создан дневной лог на 12-07; в `.cursorrules` зафиксировано правило про коммиты только по явному запросу. → Обновлены CDN-ссылки в `index.html` на Bootstrap 5.3.8 (CSS/JS) с валидными SHA-384 хешами. Переключена загрузка Vue на `vue.global.prod.js` версии 3.5.25; отображаемая версия в UI обновлена. Добавлен файл `history/2025/12-07.txt` для дневного лога. В `.cursorrules` добавлено требование: коммиты только по явной команде пользователя. ◉ Использование integrity-хешей для CDN-ресурсов демонстрирует принцип безопасности: проверка целостности предотвращает подмену библиотек и защищает от атак через скомпрометированные CDN. Фиксация версий зависимостей обеспечивает воспроизводимость сборки: проект будет работать одинаково независимо от времени развертывания. Правило коммитов только по явной команде заложило принцип контроля: пользователь полностью контролирует историю изменений, предотвращая нежелательные коммиты и обеспечивая осознанность каждого изменения в репозитории.

## Коммит c4ad8b7 от 06.12.2025 : Update daily log format: narrative style with commit references in text

## Коммит e5ae56d от 06.12.2025 : Remove style section, use Bootstrap classes only; add Bootstrap styling rule to .cursorrules
◆ В `.cursorrules` добавлено требование использовать только Bootstrap-классы без кастомных `<style>`. → В `.cursorrules` добавлен пункт о невозможности подключать собственные CSS-блоки в HTML и необходимости опираться на утилиты Bootstrap. ◉ Установлен строгий стандарт стилизации: запрет на кастомные CSS-блоки и приоритет Bootstrap-классов обеспечивает единообразие интерфейса и упрощает поддержку. Это демонстрирует принцип ограничения выбора: меньше вариантов стилизации означает меньше места для ошибок и более предсказуемый результат. Стандартизация ускоряет ревью кода и снижает риск визуальных багов из-за конфликтов стилей.

## Коммит e245cda от 06.12.2025 : Add Principles section and Vue.js 3 + Bootstrap 5 integration
◆ В `.cursorrules` появилась секция "Принципы" (UI/UX/MM) и правило обработки команд `UI:/UX:/MM:`. → В файл добавлены описания того, как агент должен фиксировать новые принципы и синхронизировать их с историей изменений. ◉ Создана система быстрого документирования принципов через команды: пользователь может моментально зафиксировать правило для конкретной области (UI, UX, MM), а агент автоматически документирует его в правильном месте. Это демонстрирует подход к управлению знаниями: принципы фиксируются в момент их формулирования, а не откладываются на потом, что критично для сохранения контекста и предотвращения потери важных решений.

## Коммит 34cd3fb от 06.12.2025 : Update logging system: switch to manual daily protocol, add .gitignore, remove Python dependencies
◆ Перешли от Python-скриптов к ручному протоколированию дневников и ввели `.gitignore`. → В `.cursorrules` описан новый регламент: структура `history/YYYY/MM-DD.txt`, ручное протоколирование по итогам дня. Создан `history/2025/12-06.txt` с полной реконструкцией диалога за день. Добавлен `.gitignore` (Python артефакты, временные файлы, системные файлы) и удалены `log_chat.py`/`__pycache__`. ◉ Упрощение процесса протоколирования демонстрирует принцип минимализма: ручное ведение истории более прозрачно и не требует внешних зависимостей, что критично для портативности проекта. Введение `.gitignore` заложило практику защиты репозитория от артефактов сборки и временных файлов, предотвращая их случайное коммит и загрязнение истории.

## Коммит 1fa4dbf от 06.12.2025 : Remove unused .ai-config.md, update theme to default, add rule for tracking unnecessary files
◆ Удалена устаревшая конфигурация и добавлено правило проактивного удаления "болтающихся" файлов. → `delete_file` удалил `.ai-config.md`, а `.cursorrules` получил новый пункт об обязательном отслеживании мусорных файлов. В `.vscode/settings.json` возвращена тема `Default Light+`, рекомендации по Atom One Light перенесены в комментарии. ◉ Установлен принцип чистоты репозитория: проактивное удаление неиспользуемых файлов предотвращает накопление "технического долга" в виде устаревших конфигураций и артефактов. Консолидация правил в единый источник (`.cursorrules`) демонстрирует принцип единственности источника истины: один файл правил проще поддерживать и обновлять, чем несколько разрозненных конфигураций.

## Коммит d6745bc от 06.12.2025 : Sync VS Code settings: update theme to Atom One Light and user preferences
◆ Синхронизированы личные предпочтения пользователя по редактору (темы, вкладки, подсветка совпадений). → В `.vscode/settings.json` выставлены `workbench.editor.showTabs="multiple"`, `editor.occurrencesHighlight="singleFile"`, `workbench.editor.tabActionLocation="right"`, тема `Atom One Light`. В `extensions.json` добавлено расширение `akamud.vscode-theme-onelight`. `.cursorrules` и `.vscode/README.md` обновлены описанием новых требований. ◉ Персонализация окружения разработки зафиксирована как часть проекта: предпочтения пользователя становятся стандартом, который ИИ-агент обязан поддерживать. Это демонстрирует баланс между гибкостью и консистентностью: личные настройки формализованы, но остаются частью версионируемой конфигурации, обеспечивая комфортную работу и предсказуемость поведения редактора.

## Коммит 433448a от 06.12.2025 : Add VS Code settings and spell checker configuration for seamless Cursor-VS Code transition
◆ Настроена синхронизация окружений Cursor ↔ VS Code для единообразной работы. → Добавлена папка `.vscode/` с подробным `settings.json` (терминал Git Bash, кодировка UTF-8, подсветка, автосохранение и т.д.). Оформлены `extensions.json`, `cspell-dict.txt` и `README.md` с рекомендациями и словарём. В `.cursorrules` добавлены требования синхронизировать настройки и расширения между средами. ◉ Установлен принцип переносимости окружения разработки: настройки редактора версионируются вместе с кодом, обеспечивая идентичную среду для всех участников независимо от используемого редактора. Это демонстрирует подход "инфраструктура как код" для локальной разработки: конфигурация среды становится частью проекта, устраняя проблему "у меня работает, у тебя нет".

## Коммит 3ce210f от 06.12.2025 : Add AI agent configuration files (.cursorrules and .ai-config.md)
◆ Заданы базовые правила работы ИИ-агента с проектом и пользователем. → Созданы `.cursorrules` и `.ai-config.md`, где описаны цели проекта, поведение ИИ-агента, требования к Git и документированию. Зафиксированы инструкции о чтении `.cursorrules` при каждом запуске, чтобы агент всегда начинал с актуальных правил. ◉ Создан фундамент для управляемой разработки: единый источник правил обеспечивает консистентность поведения ИИ-агента между сессиями, исключая расхождения в стиле работы и подходе к задачам. Это заложило принцип "правила как код": все инструкции формализованы и версионируются вместе с проектом, что критично для долгосрочной поддержки и преемственности.
